# 크라우드 내비게이터 (Crowd Navigator) — PRD

---

## ① 프로젝트 개요

**프로젝트명**: 크라우드 내비게이터 (Crowd Navigator)

**한 줄 정의**: 실시간 관광지 혼잡도를 분석해 한적한 대안 여행지를 추천하는 웹앱

**선정 배경**: 1단계에서 앱 아이디어 5개 중 이걸 선택한 이유는 두 가지였다 — ① 공모전 출품용으로 완성까지 끌고 갈 프로젝트이자 여행 혼잡 문제는 직접 겪어본 불편이라 공감대가 있었고, ② KTO 공공데이터 기반 예측 로직을 직접 설계하는 과정이 기술적으로 가장 흥미로웠기 때문. 이 두 가지는 "왜 다른 4개 아이디어가 아니라 이건가"에 대한 실제 채택 근거이지, 사후적으로 붙인 이유가 아니다.

**워킹 스켈레톤 검증 목표** (5단계 ②): "여행지를 검색하는 순간 혼잡도와 대안을 함께 확인하고 판단할 수 있다"는 핵심 가치가 실제로 작동하는지 검증. 검색→혼잡도 인지→대안 비교→저장까지 흐름이 끊기지 않는지가 유일한 검증 대상이며, 매출이나 리텐션은 이 단계의 성공 기준이 아니다.

**컨셉 스테이트먼트** (3단계 ⑤): '20~30대 국내 여행객'은 '유명 관광지가 몰려 혼잡한데 대안을 몰라 같은 곳에 계속 몰리는 문제' 때문에 힘들어한다. '크라우드 내비게이터'는 '여행지를 검색하는 순간 혼잡도와 비교 가능한 대안을 함께 보여주는 기능'으로 이를 해결하며, 기존 '혼잡도 조회형 앱'과 달리 '정보를 여행 계획 흐름 자체에 자연스럽게 통합한 점'을 갖는다.

**서비스 지역 범위**: MVP는 **서울로 한정**한다. 전국 단위 KTO 데이터를 한번에 다루면 검증 범위가 지나치게 커지고, 대안 후보 탐색·혼잡 예측 로직의 정확도 검증도 어려워지기 때문. 경쟁사 벤치마킹(3단계 ③)에서도 '서울 지금'이 서울 한정으로 시작해 성립한 사례가 있어, 지역 범위를 좁혀도 서비스로서 유효하다는 근거가 있다. 지역 확장(시/군구 단위 드롭다운·검색)은 핵심 흐름 검증 이후의 로드맵으로 ④에 Could로 등록한다.

**아키텍처 방향**: 개발 기간이 30시간 MVP가 아니라 한 달로 확보되고, 목표가 워킹 스켈레톤 검증을 넘어 공모전 출품·포트폴리오 완성도까지 포함하게 되면서, 백엔드를 사용자 응대용(Next.js)과 혼잡 예측 전담용(FastAPI, Python)으로 분리하기로 했다. 현재 Must 기능 기준으로는 이 분리가 오버스펙이라는 점을 인지하고 있으며, 그럼에도 지금 구조를 잡는 이유는 ① 향후 KTO 외 추가 공공데이터(날씨·교통 등)를 다룰 계획이 있어 나중에 마이그레이션하는 비용보다 지금 분리해두는 비용이 낮다고 판단했고, ② 공모전·이력서 목적상 파이썬 백엔드 경험 자체가 프로젝트 목표의 일부이기 때문이다. 세부 스펙은 ⑦ 기술 스택에서 다룬다.

---

## ② 문제 정의

**문제 (한 줄, 2단계 ①)**: 유명 관광지가 특정 시기에 몰려 혼잡한데, 관광객이 대안 장소나 시간을 몰라서 같은 곳에 계속 몰림

**근본원인 (2단계 5 Whys)**: 표면적으로는 "실시간 유동인구 데이터가 개인에게 없다"는 기술적 문제로 보이지만, 5단계까지 파고든 결론은 **"사람들이 여행 계획을 세울 때 혼잡도를 고려하는 습관 자체가 없다"**는 행동적 문제다. 이 구분이 중요한 이유는, 만약 진짜 원인이 데이터 부재였다면 "혼잡도 대시보드"만 만들면 됐겠지만, 진짜 원인이 습관 부재라면 서비스가 사용자의 기존 흐름(검색·계획) 안에 자연스럽게 끼어드는 방식으로 설계돼야 하기 때문이다. 이 판단이 이후 컨셉("검색하는 순간 자연스럽게 보이게")과 화면 설계(대시보드가 아니라 검색결과 화면에 통합) 전체를 관통한다.

**데이터로 검증된 부분과 검증되지 않은 부분** (1단계 ③④, 2단계 ①):
- 검증됨: "관광지" 검색은 2월·9월·10월 등 선선한 시기에 뚜렷하게 몰리는 패턴이 확인됨 → 혼잡을 사전에 피하려는 탐색 행동 자체는 계절적으로 존재
- 검증 안 됨(가설로 남음): "혼잡"이라는 단어로는 거의 검색되지 않음 → 이 문제를 적극적으로 해결하려는 의도가 얼마나 강한지, 지불 의사가 있는지는 짝 인터뷰 수준에서만 확인됐고 정량 검증은 안 됨. 이 서비스는 "검색 유입형"이 아니라 "콘텐츠·추천 노출형" 수요를 겨냥해야 한다는 게 1단계 결론이며, 이 판단이 마케팅 카피를 "혼잡 회피"가 아니라 "쾌적한 방문 시기 추천"으로 잡아야 한다는 리스크 대응으로 이어짐.

---

## ③ 타겟 사용자

**최종 페르소나 (3단계 ②D): 윤지우, 30세, 직장인**

| 항목 | 내용 |
|---|---|
| 목표 | 인파에 압도되지 않으면서도, 가고 싶은 곳과 먹고 싶은 것은 다 챙기는 여행 |
| 핵심 페인포인트 | ① 여유를 원하는 의도와 실제로 빡빡하게 채우는 행동 사이의 갭 ② 혼잡 상황에서 기다릴지 이탈할지 즉흥 판단 부담 ③ 단일 추천은 못 믿음 — 비교 가능한 여러 선택지 필요 ④ 동반자 있으면 대응 실패 시 여행 전체가 무너지는 비용 |
| 행동 패턴 | 도보면 최대 1시간 기다림, 차 이동/동반자 있으면 즉시 이탈. 매직패스·웨이팅앱 같은 "사전 확보형" 도구를 이미 적극 활용 중 |
| 기술 친숙도 | 중상 — "확실히 도움된다"는 확신이 있어야 새 앱을 설치함 |

**이 페르소나를 채택한 근거**: 실제 인터뷰(본인), AI 가상 페르소나 3명(서연=자연 힐링 성향, 준혁=효율 추구·즉시 이탈, 미래=동반자 있을 때 실패비용 증가), 데이터랩(20~30대·모바일 위주, 관광지 검색 계절성)을 종합한 결과다. 특히 "미래(가족 동반)" 요소는 참고만 하고 최종 채택하지 않았는데(4단계 Won't 근거), 이는 개인 여행자 검증을 먼저 하는 게 MVP 범위상 맞다는 판단 때문이었다 — 유모차/아동 동반 특화 기능이 Won't로 분류된 것도 같은 이유다.

**타겟 세그먼트 (1단계 ⑤)**: 20~30대 국내 여행객 중, 관광지 방문 전 정보를 검색으로 미리 찾아보는 사람들. 모바일 검색 비중이 높은 층.

---

## ④ 핵심 기능 명세 (Must 기준)

4단계 MoSCoW에서 Must로 분류된 5개 기능과 그 근거:

| 기능 | 근거 |
|---|---|
| 검색/조회 시 혼잡도 예측 표시 | HMW1("검색하는 순간 혼잡도가 자연스럽게 보여야")과 직결. 이게 없으면 서비스 자체가 성립하지 않음 |
| KTO 공공데이터 기반 혼잡 예측 로직 | 5 Whys 근본원인 대응 — "실시간 유동인구 데이터를 개인이 구할 수 없다"는 문제에 대한 현실적 기술 기반. **FastAPI(Python) 서비스가 전담**하며, pandas로 KTO 원본 데이터를 정제·집계해 rule-based 스코어링(혼잡+이동+날씨 가중합)으로 계산 |
| 혼잡 시 대안 장소 리스트 (복수 추천) | 페인포인트 "단일 추천은 못 믿는다" → 복수 비교가 필수 요구사항 |
| 대안 비교 뷰 (접근성·날씨 등 나란히) | 저니맵 핵심사용 단계 그대로. 판단 근거를 제공했을 때 지우가 "안심" 감정으로 연결됨이 확인됨 |
| 관심 장소 다건 등록/스와이프 비교 | 저니맵 페인포인트 "하나하나 넣기 귀찮다" 해소 — 없으면 온보딩 단계에서 이탈 위험 |

**Should로 분류됐지만 이번 라운드에서 화면에 이미 반영된 기능** (5단계 페르소나 검증 및 이후 대화에서 결정):
- **이동수단(도보/차) 기반 판단 지원**: 실제 인터뷰에서 확인된 행동 패턴("차면 바로 이탈, 도보면 기다림")과 직결되고, 기존 대안 스코어 로직에 파라미터 하나만 얹으면 되는 가벼운 작업이라 워킹 스켈레톤 단계에서 함께 구현하기로 결정. 프로토타입 v2에 반영 완료.
- **관심 장소 다건 등록**은 원래 Must였으나 초기 화면 시안엔 빠져 있었음 → 검증 과정에서 발견해 체크박스 다중선택 UI로 보완, v2 프로토타입에 반영 완료.
  - **구현 단계에서 하트 방식으로 변경 (2026-08-19)**: 검색 결과에서 체크박스로 여러 곳을 고른 뒤 하단 버튼으로 일괄 담는 방식을, 항목별 하트 버튼으로 바꿨다. 세어 보면 N곳 담을 때 체크박스는 N+1 탭이고 하트는 N 탭이다. 다중선택을 넣은 이유가 "하나하나 넣기 귀찮다"였으므로, 확인 단계를 없애는 쪽이 그 목적에 더 맞는다. 잘못 담았을 때 같은 자리에서 바로 뺄 수 있다는 이점도 있다(기존에는 관심 장소함까지 가야 했다). 장소 상세화면의 "담기" 버튼도 같은 이유로 이름 옆 하트로 통합했다 — 담긴 상태를 문장으로 설명하면 한 줄이 더 필요하고, 상태 표시와 조작이 떨어져 있게 된다.

**Should로 유지, 이번 라운드에서는 보류한 기능**:
- **동행자 공유 링크**: 페인포인트4(동반자 있으면 실패비용 커짐)와 연결되지만, "핵심 흐름(검색→혼잡도→대안→저장) 없이도 서비스는 작동 가능"하다는 판단에 따라 이번 화면 수정 라운드에서는 Could로 하향 조정. 개인 사용자군 검증을 먼저 마친 뒤 확장 단계에서 추가. (※ 4단계 원본 파일은 아직 Should로 남아있어 별도 업데이트 필요)
- **혼잡 예측 기반 방문 시간대 사전 알림/찜하기**: 매직패스·웨이팅앱 같은 사전 확보형 도구를 이미 선호하는 페르소나 행동과 부합해 가치는 있으나, 상세화면의 "몇 시 이후 여유" 텍스트만으로도 핵심 판단은 가능하다고 보고 Could로 분류, 핵심기능 이후 고려.

**4단계 ③ 검증에서 추가 제안됐던 3개 항목의 최종 처리 결과**:
- **맛집 등 카테고리 확장**: 폐기. 관광지 혼잡도 비교라는 핵심 차별점과 무관하다고 판단해 백로그에서 완전히 제외.
- **일정 과밀도 체크**(선택한 장소들의 혼잡 시간대가 겹치는지 알림): Could로 유지하되 후순위. 페인포인트1(여유 원함 vs 빡빡한 일정 갭)에 대응하지만, 핵심 흐름 검증에는 필수가 아니라고 판단.
- **예상 혼잡 해소 시각 표시**: 별도 기능으로 만들지 않고 폐기. 상세화면의 시간대별 혼잡도 그래프가 이미 "언제 여유로워지는지"를 시각적 지표로 보여주고 있어, 별도 텍스트("몇 시부터 해소")를 추가하지 않아도 같은 판단이 가능하다고 결론.

**신규 로드맵 항목 (Could)**:
- **지역 확장 (시/군구 드롭다운 또는 검색)**: MVP는 서울 한정이라 지역 선택 UI 자체가 불필요하지만, 전국 확장 시엔 필요해지는 기능. 지금은 백로그에만 등록해두고, 핵심 흐름(서울 기준) 검증이 끝난 뒤 착수.

**Won't로 제외한 기능과 이유** (4단계 ①): 통신사급 실시간 유동인구(애초에 접근 불가능한 데이터), 예약/결제 연동(혼잡도 비교라는 차별점과 무관), AI 자동 일정 생성(지우의 "계획을 강요받지 않는" 즉흥성 유지 니즈와 정면 상충), 유모차/아동 동반 특화(최종 페르소나가 개인 여행자로 확정됨에 따라 범위 밖).

---

## ⑤ 화면 구성 및 사용자 흐름

핵심 사용자 흐름 (5단계 ②): 검색 입력 → 검색 결과(장소별 혼잡도) 확인 → 장소 상세 진입 → (혼잡 시) 대안 비교로 분기 → 대안 선택 시 장소 상세로 순환 → 관심 장소함에 저장 → 관심 장소함에서 재조회

| 화면 | 핵심 요소 | 설계 근거 |
|---|---|---|
| 검색 진입 | 텍스트 입력 필드, 검색 버튼 | 최소 진입점 — 이 시점엔 아무 데이터도 안 보이게 해 첫 화면 로딩 부담을 없앰. MVP는 서울 한정이라 지역 선택 UI는 없고 장소명 검색만 제공 |
| 검색 결과 | 장소별 혼잡도 리스트(지수 + 등급 + 게이지), **항목별 담기 하트** | KTO 데이터 기반 혼잡 예측을 리스트로 노출(Must). 한적한 순으로 정렬한다 — 붐비는 곳을 위에 두면 이 서비스의 존재 이유와 어긋난다. 담기는 하트를 눌러 즉시 반영되고 다시 누르면 빠진다(④의 변경 근거 참고). 담긴 상태는 하트의 채움으로 보여 중복 담기를 UI 단에서 막고, DB 의 (user_id, place_id) UNIQUE 가 최종 방어선이다 |
| 장소 상세 | 혼잡도 수치/등급, 시간대별 그래프, 기본정보(이용시간·휴무일·입장료·주차), 이름 옆 **담기 하트**, "근처 가볼 만한 곳" 버튼, 길찾기(카카오맵·네이버 지도) | 혼잡도가 기준 이상이면 버튼을 진하게 강조하고 안내 문구를 함께 띄운다(분기 로직). **도보/차 토글은 대안 비교 화면으로 옮겼다** — 후보가 여러 개 있어야 기준을 바꿨을 때 순위가 달라지는 걸 볼 수 있고, 장소 하나뿐인 상세 화면에서는 눌러도 화면이 변하지 않았다 |
| 대안 비교 | 대안 리스트(혼잡도+거리), **도보/차 토글**(헤더 우측 — 후보 *범위*를 3km/15km로 바꿈), **한적한순/가까운순 정렬 토글**(리스트 바로 위 — 같은 후보의 *순서*만 바꿈) | "단일 추천 못 믿는다"는 페인포인트에 대응. 다만 근거는 **종합 점수를 보여주는 방식이 아니라, 서로 다른 두 기준으로 직접 줄 세워 보게 하는 방식**으로 제공한다(아래 "구현 단계에서 확정한 사항" 참고). 클릭 시 상세화면으로 재진입해 3↔4단계 왕복 가능 |
| 관심 장소함 | 담아둔 장소 리스트, 리스트/글랜스(스와이프) 모드 전환 | 저니맵에서 확인된 "여러 장소를 모아 훑어보고 싶다"는 니즈에 대응 |

**화면 검증 결과 요약** (5단계 ④, 최종본): 핵심 페인포인트(혼잡도 노출, 복수 비교, 데이터 투명성)는 O로 확인됐고, 남은 갭 중 다중선택·이동수단 토글은 이번 라운드에 반영, 동행자 공유·사전알림은 의도적으로 Could 보류, 재방문 훅은 워킹 스켈레톤 스코프 밖으로 명시적으로 남겨둠.

### 구현 단계에서 확정한 사항 — 대안 비교의 "근거 제시" 방식 (2026-08-20 변경)

원안은 **종합 점수와 그 계산식을 카드에 노출**하는 방식이었다(`혼잡 67 × 0.6 + 접근성 68 × 0.4 = 68점`). 이를 **점수 비노출 + 서로 다른 두 기준으로 직접 줄 세우기**로 바꾼다.

**바꾼 이유**

1. **점수는 검산이 불가능하다.** 페인포인트 ③은 "단일 추천은 못 믿는다"인데, `68점`은 왜 68인지 확인할 방법이 없어 불신을 하나 더 만든다. 반면 혼잡 지수와 거리는 카드에 찍힌 숫자와 정렬 순서가 맞물려 있어 눈으로 검산된다.
2. **가중치 이름과 실제 동작이 어긋났다.** 실측(종로구·도보 3km)에서 혼잡도는 24~46으로 뭉치고 접근성은 28~68로 벌어져, 가중치가 혼잡 0.6인데도 **점수 차이는 접근성이 더 많이 만들었다**. 그 결과 "혼잡 우선" 점수가 사실상 거리순으로 굴러갔다.
3. **라벨을 동작에 맞추는 건 답이 아니었다.** 점수순에 "가까운순"이라 붙이면 1.25km가 0.69km보다 위에 오는 걸 사용자가 카드의 km으로 즉시 발견한다.

**확정 구조 — 선별과 정렬을 분리한다**

| 역할 | 무엇으로 | 노출 |
|---|---|---|
| 후보 **범위** | 도보/차 토글 (3km / 15km) | 헤더 우측 |
| 후보 **선별** (열몇 곳 → 5곳) | 가중합 점수 | **비노출** (내부 계산) |
| 후보 **순서** | 한적한순 / 가까운순 (기본: 한적한순) | 리스트 바로 위 |

선별까지 정렬 기준으로 하지 않는 이유: 한적한순으로 5곳을 뽑으면 전부 멀어질 수 있고, 그러면 가까운순 탭이 "먼 것들 중에 가까운 순"이 되어 무의미해진다. 균형 잡힌 5곳을 남겨야 두 관점이 다 살아난다.

기본 정렬을 한적한순으로 두는 근거: 도보/차 토글이 이미 거리 상한을 보장하므로 **범위는 필터가 지키고 순서는 서비스 정체성이 정한다.**

카드에 남는 것은 **혼잡 지수(수치·등급·게이지)와 거리(역에서 도보 분 포함)**뿐이다. 1순위 카드 머리도 "1순위 추천"이 아니라 정렬 기준을 그대로 적는다("가장 한적한 곳"/"가장 가까운 곳") — 지금 순서는 우리가 추천한 게 아니라 사용자가 고른 기준이다.

**가중치 재조정은 FastAPI 이관 시점으로 미룬다.** 점수가 순서를 결정하지 않게 됐으므로 불일치가 사용자에게 미치는 영향이 사라졌고, 정식 스코어링은 예측 서비스 몫이다(⑦).

---

## ⑥ 데이터 구조

6단계에서 화면을 역분석해 도출한 테이블 구조 (Supabase 기준). 당초 "혼잡도는 테이블화하지 않고 실시간 계산"으로 결정했으나(6단계 원안), 개발 기간이 한 달로 확보되고 FastAPI 서비스를 별도로 두기로 하면서 **이 결정을 뒤집는다** — 매 사용자 요청마다 계산하는 대신, FastAPI가 주기적으로 미리 계산해 저장해두고 Next.js는 그 결과만 읽는 구조로 바꾼다.

**Place** (관광지 마스터)

| 컬럼 | 타입 | 출처 |
|---|---|---|
| id | uuid (PK) | 전체 화면 공통 참조 키 |
| kto_content_id | text (UNIQUE) | KTO TourAPI의 `contentid`. **구현 단계 추가** — 시드 스크립트를 여러 번 돌려도 같은 장소가 중복 생성되지 않게 하는 키. 화면에는 노출되지 않음 |
| name / name_en | text | 검색결과·상세·대안카드 |
| category | text | 검색 필터링용 (현재 비노출, 확장 대비) |
| district | text | 구/군 단위 (예: 종로구). 지금은 화면에 안 쓰지만, address에서 매번 파싱하는 대신 미리 분리해두면 나중에 시/군구 드롭다운 필터를 컬럼 하나로 바로 구현할 수 있음 |
| address | text | 지도 링크 생성용 |
| lat / lng | numeric | 지도 링크·거리 계산·대안 후보 탐색 |
| access_desc | text | 상세/대안카드 '접근성' |
| fee | text | 상세/대안카드 '입장료' |
| use_time | text | **구현 단계 추가** — 이용시간. KTO `detailIntro2` 에서 받는다 |
| rest_date | text | **구현 단계 추가** — 휴무일. 화면 표시뿐 아니라 **예측에도 반영**한다(아래 참고) |
| parking | text | **구현 단계 추가** — 주차 가능 여부 |
| info_center | text | **구현 단계 추가** — 문의처 |

> **휴무일을 예측에 반영하는 이유 (2026-08-19)**: 문 닫은 장소를 "여유롭다"고 추천하는 것이 이 서비스에서 가장 큰 사고다. 궁·박물관은 월요일이나 화요일에 휴관하는데(경복궁 화요일, 창덕궁·덕수궁 월요일), 집중률 예측치는 그 사실을 모른다. 그래서 배치가 `rest_date` 를 읽어 휴무일의 시간대별 혼잡도를 전부 0 으로 만든다.
> 다만 `rest_date` 는 자유 문장("매주 월요일", "1월 1일, 설날 당일")이라 **요일 단위 정기 휴무만** 읽는다. 못 읽은 문장은 "휴무일 모름"으로 두고 예측을 그대로 진행한다 — 잘못 읽어 문 연 날을 닫혔다고 하는 쪽이 더 나쁘다.
>
> **이 정보를 채우지 못하는 장소가 있다**: 집중률 API 에만 있고 TourAPI 에는 없는 장소(경복궁·창덕궁·종묘·광화문 등)는 `detailIntro2` 를 부를 `contentid` 자체가 없다. 이런 곳은 `predictor/app/landmarks.py` 에 직접 적어 넣는다. 목록은 짧게 유지한다 — 길어지면 결국 손으로 관리하는 장소 마스터가 되어버린다.

**UserFavorite** (관심 장소함)

| 컬럼 | 타입 | 출처 |
|---|---|---|
| id | uuid (PK) | 내부 관리용 |
| user_id | uuid (FK → auth.users) | 담은 사용자 — Supabase 익명 인증(Anonymous Auth)으로 발급된 ID. 가입 UI 없이 기기 내에서 자동 유지되며, 나중에 실 로그인을 붙이면 이 익명 계정을 이메일 계정으로 승격(link)하는 방식으로 확장 |
| place_id | uuid (FK → Place) | 상세화면 담기, 결과화면 다중담기 |
| added_at | timestamptz | 등록순 정렬 대비 |

제약: **(user_id, place_id) 조합에 유니크 제약**을 걸어, 같은 장소를 두 번 담는 요청이 들어와도 DB 레벨에서 중복 저장이 안 되게 막는다. (화면 단 방지는 ⑤ 참고)

**CongestionForecast** (혼잡 예측 결과, 신규)

| 컬럼 | 타입 | 출처 |
|---|---|---|
| id | uuid (PK) | 내부 관리용 |
| place_id | uuid (FK → Place) | 상세화면 시간대별 그래프, 검색결과 리스트 혼잡도 배지 |
| hour_slot | int | 상세화면 그래프 x축 (시간대) |
| congestion_pct | int | 검색결과 배지, 상세화면 그래프 y값, 대안비교 스코어링 입력값 |
| forecast_date | date | 예측 기준일 — 이 값 기준으로 최신 예측치인지 판단 |
| computed_at | timestamptz | FastAPI가 이 행을 언제 계산해 넣었는지 (디버깅·모니터링용) |

제약: **(place_id, forecast_date, hour_slot) 조합에 유니크 제약**을 건다. **구현 단계 추가** — 배치가 하루 1~2회 도는데 이 제약이 없으면 같은 장소·같은 날·같은 시간대 예측이 실행할 때마다 중복 누적되고, 화면에서 어느 행을 써야 할지 판단할 수 없게 된다. 제약이 있으면 배치가 upsert(있으면 갱신, 없으면 삽입)로 깔끔하게 동작한다.

이 테이블은 **FastAPI가 하루 1~2회 주기적으로 계산해서 채워 넣고, Next.js는 조회만** 한다. Next.js가 매 요청마다 KTO 원본 데이터를 다시 계산하지 않으므로 응답 속도가 빠르고, FastAPI 서비스가 일시적으로 죽어도 최근 예측치는 테이블에 남아있어 서비스가 완전히 멈추지 않는다는 장점도 있다.

**관계**: users와 Place는 각각 UserFavorite과 1:N 관계를 맺고, 그 조합으로 users↔Place는 결과적으로 N:M(다대다) 관계가 된다. Place와 CongestionForecast는 1:N(한 장소가 시간대별로 여러 예측 행을 가짐) 관계다. 화면에서 관심장소함을 보여줄 땐 UserFavorite에서 해당 사용자의 place_id 목록을 먼저 찾고, 그 id로 Place를 조회하는 순서로 동작한다.

### 구현 단계에서 확정한 사항 (2026-08-18, Supabase 적용 완료)

실제 테이블을 생성하면서 위 설계에 더해 확정한 내용이다. 설계 자체를 바꾼 게 아니라, PostgreSQL에서 이 설계를 안전하게 굴리기 위해 필요한 것들이다.

| 항목 | 결정 | 이유 |
|---|---|---|
| 테이블명 표기 | `place` / `user_favorite` / `congestion_forecast` (snake_case) | PostgreSQL에서 대문자 이름은 이후 모든 쿼리에 큰따옴표를 강제한다(`"UserFavorite"`). 하나만 빠뜨려도 에러라 실수가 잦아 소문자로 통일 |
| 삭제 전파 | 모든 FK에 `on delete cascade` | 장소가 지워지면 그 장소의 즐겨찾기·예측치도 함께 정리. 없으면 참조가 끊긴 유령 행이 남는다 |
| 값 범위 검증 | `hour_slot` 0~23, `congestion_pct` 0~100에 `check` 제약 | FastAPI 배치가 계산을 잘못해도 이상값이 저장되지 않게 DB에서 막는다 — 버그를 저장 시점에 차단 |
| 인덱스 | `user_favorite(user_id)`, `congestion_forecast(place_id, forecast_date)` | 관심 장소함 조회와 상세화면 시간대별 그래프의 실제 조회 패턴에 맞춤 |

**RLS(Row Level Security) 정책**: Supabase의 anon 키는 브라우저에 노출되는 값이므로, RLS를 걸지 않으면 누구나 전체 테이블을 읽고 쓸 수 있다. 세 테이블 모두 RLS를 활성화했다.

| 테이블 | 읽기 | 쓰기 |
|---|---|---|
| place | 누구나 (anon·authenticated) | 클라이언트 불가 — 시드/관리자(service_role)만 |
| congestion_forecast | 누구나 (anon·authenticated) | 클라이언트 불가 — FastAPI(service_role)만 |
| user_favorite | **본인 행만** (`auth.uid() = user_id`) | **본인 행만** 추가·삭제 |

`user_favorite`은 조회 자체가 본인 것으로 제한되므로 남의 관심 장소함은 읽을 수도 없다. 익명 인증으로 로그인한 사용자도 `authenticated` 롤을 받으므로 이 정책이 그대로 적용된다.

**여전히 테이블화하지 않는 데이터**: 날씨는 실시간성이 강해 별도 테이블 대신 API 호출로 처리한다(계산 로직이 아니라 외부 데이터 그 자체이기 때문에 CongestionForecast와는 성격이 다름). 이동시간(예: "지하철 12분")은 검증 과정에서 발견된 진짜 누락 항목이지만, 개발 진행하면서 실제 필요성을 확인하기로 결정 — 지금은 목업 값으로 두고, 장소 상세화면의 **길찾기 링크**가 이미 정확한 경로·시간 안내를 대신하고 있어 급하지 않음.

> **구현 단계 확정 (2026-08-19)**: 길찾기는 **카카오맵과 네이버지도 두 곳을 나란히** 둔다. 사람마다 쓰는 지도 앱이 달라 하나만 두면 그 앱을 안 쓰는 사람은 주소를 복사해 옮겨 붙이는 수고를 하게 된다. 좌표를 우선 쓰고(주소는 검색 결과가 여러 개일 수 있지만 좌표는 한 점을 가리킨다), 좌표가 없는 장소는 이름·주소 검색으로 대체한다.
> **티맵은 넣지 않는다** — 웹 지도가 없어 `tmap://` 앱 스킴뿐이고, 데스크톱에서는 눌러도 아무 일이 일어나지 않는다. 죽은 버튼은 없는 버튼보다 나쁘다(사용자가 앱 탓인지 자기 탓인지 알 수 없다). 앱으로 감싸게 되면 그때 추가한다. GPS 좌표(사용자 현재위치)는 **서버에 저장하지 않고, 브라우저 Geolocation API로 요청 시점에만 받아 "내 위치에서 거리" 계산에 즉시 사용 후 폐기**한다.

---

## ⑦ 기술 스택 및 비기능 요구사항

### 기술 스택 — 투 서비스 구조

당초 "Next.js Route Handlers만으로 충분"하다고 봤던 결정을, 개발 기간이 한 달로 확보되고 공모전·포트폴리오 목적이 명확해지면서 변경한다. 사용자 응대(Next.js)와 혼잡 예측 전담(FastAPI)을 분리한다.

| 구분 | 스택 | 역할 |
|---|---|---|
| 프론트엔드 | Next.js(App Router) + TypeScript, Vercel 배포 | 화면, 검색/즐겨찾기 CRUD, 익명 인증 — 사용자와 직접 맞닿는 모든 것 |
| 예측 서비스 | FastAPI(Python), Render/Railway 배포 | KTO 공공데이터 정제(pandas)·혼잡 예측·대안 스코어링 전담. PRD ④의 Must 기능인 "KTO 공공데이터 기반 혼잡 예측 로직"을 실질적으로 구현하는 곳 |
| 예측 로직 | Rule-based 스코어링 우선, 여유 있으면 scikit-learn 회귀/시계열 모델 추가 | Must 기준 충족엔 rule-based로 충분. 통계 모델 추가는 시간이 남을 때의 확장 항목 |
| 주기 실행 | FastAPI 내장 APScheduler 또는 별도 cron, 하루 1~2회 | 실시간 계산이 아니라 사전 계산 — Supabase의 CongestionForecast 테이블에 결과 저장 |
| 데이터베이스 | Supabase (PostgreSQL) | Place / UserFavorite / CongestionForecast 3테이블, 익명 인증 내장 |

**통신 방식**: 브라우저가 FastAPI를 직접 호출하지 않는다. Next.js 서버(Route Handler)가 중간에서 FastAPI를 호출하고, 결과를 다시 브라우저에 내려준다. 이렇게 하면 CORS 설정이 필요 없고, FastAPI 서버 주소가 외부에 노출되지 않아 보안 관리가 단순해진다.

**이 결정이 앞선 판단과 다른 이유**: 6단계 원안과 이전 라운드의 "Place+UserFavorite 2테이블, 백엔드는 Next.js로 충분" 결정은 30시간 워킹 스켈레톤 전제에서 나온 것이었다. 개발 기간이 한 달로 늘고 목표에 이력서·공모전 완성도가 명시적으로 추가되면서, 오버엔지니어링 방지 원칙보다 "실제 서비스 분리 경험"의 가치가 커졌다고 판단해 구조를 바꿨다.

### 비기능 요구사항

원본 1~6단계 워크시트엔 이 섹션에 해당하는 내용이 없어, 개발 착수 전 확인 인터뷰를 통해 항목별로 직접 확정했다.

| 항목 | 요구사항 | 근거 (확정 답변) |
|---|---|---|
| 트래픽/성능 기준 | 별도의 캐싱·CDN·부하분산 설계 불필요. Vercel 기본 배포로 충분 | "개인 테스트 수준(~100명)"으로 확정 — 이 규모에선 성능 최적화에 시간을 쓰는 게 오히려 과설계 |
| 인증 방식 | Supabase 익명 인증(Anonymous Auth) 사용. 가입 UI 없이 기기 내에서 uid 자동 유지, 나중에 이메일 로그인으로 승격 가능한 구조로 설계 | "익명 인증으로 시작(가입 없이 기기 내 유지)"로 확정 — 페르소나가 "확신 있어야 설치"하는 성향이라 가입 장벽을 낮추는 게 유리하다는 판단과 일치 |
| 위치정보 처리 | 브라우저 Geolocation API로 GPS 좌표를 요청, "내 위치에서 거리"를 자동 계산. 좌표는 서버에 저장하지 않고 요청 시점 계산에만 사용 후 즉시 폐기. 권한을 거부하면 거리 표시만 생략하고, 검색·혼잡도 조회는 그대로 정상 작동 | "GPS 기반 자동 계산"으로 확정. 서비스 지역을 서울로 한정하기로 하면서 "지역 선택 폴백"은 필요 없어짐 — 검색 자체는 GPS 없이도 장소명만으로 되기 때문에, 권한 거부 시 그냥 거리 정보만 안 보여주면 됨 |
| 지역 범위 | MVP는 서울 한정, 지역 선택 UI 없음 | 전국 단위로 시작하면 검증 범위가 커지고 예측 로직 정확도 검증도 어려워짐. 지역 확장(시/군구 드롭다운)은 ④에 Could로 등록해 이후 로드맵으로 분리 |
| 개인정보 최소 수집 | 익명 인증 특성상 이메일 등 개인정보 자체를 수집하지 않음. 위치정보도 저장하지 않음 | 위 두 항목(인증·위치정보) 결정에서 자동으로 도출됨 |
| 데이터 신뢰성 | KTO 공공데이터 갱신 주기를 UI에 명시하고, 예측치임을 표기 | 1단계 리스크 항목 — "공공데이터 특성상 실시간성보다 예측치 성격이 강함"을 숨기지 않아야 신뢰 훼손을 막을 수 있음 |
| 서비스 가용성 | FastAPI 서비스가 일시적으로 다운돼도 Next.js·검색·즐겨찾기 기능은 정상 작동해야 함 | FastAPI는 CongestionForecast 테이블에 결과를 미리 써두는 구조라, FastAPI가 죽어도 Next.js는 마지막으로 저장된 예측치를 그대로 읽을 수 있음 — 두 서비스로 나누면서 생기는 장애 지점을 최소화하는 설계 |
| 확장 용이성 | **구조 확장과 성능 최적화를 구분한다.** 백엔드를 FastAPI로 분리하고 3테이블 구조로 가는 건(구조 확장) 이미 결정했고, 캐싱·CDN 같은 순수 성능 최적화는 트래픽이 실제로 늘기 전까진 계속 미룬다 | 이 프로젝트의 목표가 제품 최적화만이 아니라 "공모전 출품 + 파이썬 백엔드 경험을 이력서에 남기는 것"까지 포함한다는 걸 명시적으로 인정한다. 순수 워킹 스켈레톤 검증 관점에서만 보면 Next.js 하나로도 충분해 이 구조 확장은 오버엔지니어링이 맞지만, 개발 기간이 한 달로 확보되면서 그 비용(배포 지점 2곳, 장애 지점 증가, 디버깅 복잡도)을 감당할 여유가 생겼다고 판단해 의도적으로 선택했다 |

**해결된 이슈**: 이전에 남아있던 "위치 권한 거부 시 폴백 UX 미확정" 문제는 서비스 지역을 서울로 한정하기로 하면서 자연스럽게 해소됐다 — 별도 지역선택 화면 없이, 권한 거부 시 거리 정보만 생략하면 된다.

---

## ⑧ 성공 지표

워킹 스켈레톤 단계의 목표가 "매출 극대화가 아니라 핵심 가치 검증"(1단계 ⑤, 5단계 ②)이므로, 성공 지표도 매출보다 행동 지표를 우선한다.

**정성 지표 (페르소나 검증 기준 유지)**:
- 5단계 페르소나 검증표에서 O로 확인된 4개 항목(HMW1 반영, 복수 비교, 사전 판단 지원, 데이터 투명성)이 실제 사용자 테스트에서도 동일하게 확인되는지

**정량 지표 (퍼널 전환)**:
- 검색 → 결과 확인 → 상세 진입 → (혼잡 시) 대안 비교 → 관심 장소 담기까지 이어지는 흐름의 단계별 이탈률
- 다중선택 담기 기능 사용 비율 (초기 등록 피로도가 실제로 줄었는지의 대리 지표)

**규모 지표 (1단계 SOM 기준)**: 1인 MVP 기준 1년 내 월간활성사용자(MAU) 1,000~5,000명 수준을 현실적 목표로 삼되, 이는 AI 모의 추정치이며 실제 수치가 아니므로 반드시 실사용 데이터로 재검증이 필요하다.

**의도적으로 지금 측정하지 않는 지표**: 재방문율/리텐션은 5단계 검증에서 "워킹 스켈레톤 스코프 밖"으로 명시했으므로, 이번 단계 성공 여부 판단에는 포함하지 않는다.
