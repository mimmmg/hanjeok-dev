# CLAUDE.md — 크라우드 내비게이터(한적)

이 파일은 Claude Code가 이 저장소에서 작업할 때 따라야 할 규칙이다.
제품 의사결정의 원본 근거는 항상 `PRD.md`이며, 이 문서와 PRD가 충돌하면 PRD를 우선하고 사용자에게 알린다.

---

## 1. 프로젝트 개요

**한 줄 정의**: 서울 관광지의 혼잡도를 예측해 한적한 대안 여행지를 함께 보여주는 웹앱.

### 문제 정의 (PRD ②)

- **표면 문제**: 유명 관광지가 특정 시기에 몰려 혼잡한데, 관광객이 대안 장소나 시간을 몰라 같은 곳에 계속 몰린다.
- **근본 원인 (5 Whys 결론)**: "실시간 유동인구 데이터가 없다"는 기술 문제가 아니라, **"여행 계획을 세울 때 혼잡도를 고려하는 습관 자체가 없다"**는 행동 문제다.
- **이 구분이 설계 전체를 지배한다**: 원인이 데이터 부재였다면 "혼잡도 대시보드"를 만들면 됐지만, 습관 부재이므로 서비스는 **사용자의 기존 흐름(검색·계획) 안에 끼어드는 방식**으로 설계돼야 한다. 그래서 별도 대시보드 화면이 아니라 **검색 결과 화면에 혼잡도를 통합**한다. 새 화면을 제안할 때 이 원칙을 어기지 않는지 먼저 확인할 것.
- 데이터 검증 상태: "관광지" 검색의 계절성(2·9·10월 집중)은 확인됨. 반면 "혼잡" 키워드 검색은 거의 없어, 검색 유입형이 아니라 **콘텐츠·추천 노출형** 수요를 겨냥해야 한다. 따라서 카피는 "혼잡 회피"가 아니라 **"쾌적한 방문 시기 추천"** 톤으로 쓴다.

### 타겟 페르소나 (PRD ③)

**윤지우, 30세, 직장인 / 20~30대 국내 여행객, 모바일 검색 비중 높음**

| 항목 | 내용 |
|---|---|
| 목표 | 인파에 압도되지 않으면서도, 가고 싶은 곳과 먹고 싶은 것은 다 챙기는 여행 |
| 페인포인트 | ① 여유를 원하는 의도와 빡빡하게 채우는 행동의 갭 ② 혼잡 상황에서 기다릴지 이탈할지 즉흥 판단 부담 ③ **단일 추천은 못 믿음 — 비교 가능한 복수 선택지 필요** ④ 동반자가 있으면 대응 실패 시 여행 전체가 무너짐 |
| 행동 패턴 | 도보면 최대 1시간 대기, 차 이동·동반자 있으면 즉시 이탈. 매직패스·웨이팅앱 같은 "사전 확보형" 도구를 이미 적극 활용 |
| 기술 친숙도 | 중상 — "확실히 도움된다"는 확신이 있어야 새 앱을 설치 |

가족·아동 동반 사용자는 **의도적으로 범위 밖**(Won't). 개인 여행자 검증이 먼저다.

### 핵심 가치 & 검증 목표

- **컨셉 스테이트먼트**: 20~30대 국내 여행객의 '유명 관광지가 혼잡한데 대안을 몰라 같은 곳에 몰리는 문제'를, '여행지를 검색하는 순간 혼잡도와 비교 가능한 대안을 함께 보여주는 기능'으로 해결한다. 기존 혼잡도 조회형 앱과 달리 **정보를 여행 계획 흐름 자체에 통합**한 점이 차별점이다.
- **워킹 스켈레톤 검증 목표**: `검색 → 혼잡도 인지 → 대안 비교 → 저장` 흐름이 끊기지 않는지가 **유일한 검증 대상**. 매출·리텐션은 이 단계의 성공 기준이 아니다.
- **핵심 사용자 흐름 (PRD ⑤)**: 검색 입력 → 검색 결과(장소별 혼잡도) → 장소 상세 → (혼잡 시) 대안 비교 분기 → 대안 선택 시 상세로 순환 → 관심 장소함 저장 → 재조회.

---

## 2. 기술 스택 (고정 — 임의 변경 금지)

아래 스택은 PRD ⑦에서 확정된 결정이다. **임의로 다른 스택·라이브러리·배포처로 바꾸지 않는다.** 바꿔야 할 이유가 보이면 먼저 근거를 설명하고 확인을 받는다.

| 구분 | 스택 | 역할 |
|---|---|---|
| 프론트엔드 | **Next.js (App Router) + TypeScript + Tailwind CSS**, Vercel 배포 | 화면, 검색/즐겨찾기 CRUD, 익명 인증 — 사용자와 맞닿는 모든 것 |
| 예측 서비스 | **FastAPI (Python) + pandas**, Render/Railway 배포 | KTO 공공데이터 정제·혼잡 예측·대안 스코어링 전담 |
| 예측 로직 | Rule-based 스코어링 (혼잡+이동+날씨 가중합) 우선 | Must 충족엔 rule-based로 충분. scikit-learn 회귀/시계열은 시간이 남을 때의 확장 항목 |
| 배치 실행 | APScheduler 또는 cron, **하루 1~2회** | 실시간 계산이 아니라 사전 계산 → `CongestionForecast` 테이블에 저장 |
| 데이터베이스 | **Supabase (PostgreSQL)**, 익명 인증(Anonymous Auth) | 3테이블 구조 |

### 지켜야 할 아키텍처 제약

- **브라우저는 FastAPI를 직접 호출하지 않는다.** Next.js 서버(Route Handler / Server Component)만 FastAPI를 호출하고, 결과를 브라우저에 내려준다. FastAPI 주소·키는 서버 전용 환경변수로만 두고, `NEXT_PUBLIC_` 접두사를 붙이지 않는다. (CORS 불필요 + 서버 주소 비노출)
- **Next.js는 혼잡도를 계산하지 않는다.** 계산은 FastAPI 배치가 하고, Next.js는 `CongestionForecast`를 **조회만** 한다.
- **FastAPI가 죽어도 Next.js·검색·즐겨찾기는 정상 동작해야 한다.** 예측치는 테이블에 남아 있으므로, 마지막 저장분을 읽어 서비스가 멈추지 않게 한다. 예측 데이터 부재/노후 시의 UI 처리는 항상 고려한다.
- **데이터 신뢰성 표기**: 화면에 KTO 데이터 갱신 주기와 "예측치임"을 명시한다. 실시간처럼 보이게 하지 않는다.
- **위치정보**: 브라우저 Geolocation API로 요청 시점에만 받아 "내 위치에서 거리" 계산에 쓰고 즉시 폐기한다. **서버·DB에 절대 저장하지 않는다.** 권한 거부 시 거리 표시만 생략하고 검색·혼잡도 조회는 그대로 동작한다.

---

## 3. 데이터 구조 (PRD ⑥ / Supabase)

### Place — 관광지 마스터

| 컬럼 | 타입 | 용도 |
|---|---|---|
| id | uuid (PK) | 전체 화면 공통 참조 키 |
| kto_content_id | text (UNIQUE) | KTO TourAPI `contentid`. 시드 재실행 시 중복 생성 방지용. 화면 비노출 |
| name / name_en | text | 검색결과·상세·대안카드 |
| category | text | 검색 필터링용 (현재 비노출, 확장 대비) |
| district | text | 구/군 단위 (예: 종로구). 지금은 미노출이지만 address 파싱 대신 미리 분리 — 향후 시/군구 필터를 컬럼 하나로 구현 |
| address | text | 지도 링크 생성용 |
| lat / lng | numeric | 지도 링크·거리 계산·대안 후보 탐색 |
| access_desc | text | 상세/대안카드 '접근성' |
| fee | text | 상세/대안카드 '입장료' |

### UserFavorite — 관심 장소함

| 컬럼 | 타입 | 용도 |
|---|---|---|
| id | uuid (PK) | 내부 관리용 |
| user_id | uuid (FK → `auth.users`) | Supabase 익명 인증으로 발급된 ID. 가입 UI 없이 기기 내 자동 유지, 이후 이메일 계정으로 승격(link) 가능 |
| place_id | uuid (FK → Place) | 상세화면 담기, 결과화면 다중담기 |
| added_at | timestamptz | 등록순 정렬 대비 |

**제약**: `(user_id, place_id)` 조합에 **UNIQUE 제약**. 같은 장소를 두 번 담는 요청이 와도 DB 레벨에서 중복 저장을 막는다. 화면 단에서도 이미 담긴 장소는 체크박스 비활성화/"이미 담김" 표시로 중복을 1차 차단한다(DB 제약은 최종 방어선이지 유일한 방어선이 아니다).

### CongestionForecast — 혼잡 예측 결과

| 컬럼 | 타입 | 용도 |
|---|---|---|
| id | uuid (PK) | 내부 관리용 |
| place_id | uuid (FK → Place) | 상세화면 시간대별 그래프, 검색결과 혼잡도 배지 |
| hour_slot | int | 상세 그래프 x축 (시간대) |
| congestion_pct | int | 검색결과 배지, 그래프 y값, 대안비교 스코어링 입력값 |
| forecast_date | date | 예측 기준일 — 최신 예측치 판단 기준 |
| computed_at | timestamptz | FastAPI가 이 행을 계산해 넣은 시각 (디버깅·모니터링용) |

**제약**: `(place_id, forecast_date, hour_slot)` 조합에 **UNIQUE 제약**. 배치가 재실행돼도 중복 누적 없이 upsert로 갱신된다. `hour_slot`은 0~23, `congestion_pct`는 0~100 `check` 제약.

**쓰기 주체는 FastAPI 배치뿐이고, Next.js는 조회만 한다.**

### 실제 DB 상태 (2026-08-18 적용 완료)

- Supabase 프로젝트: **`hanjeok-dev`** / ref `nqnxvhvozzstzloymovr` / 리전 ap-northeast-2(서울)
- 테이블명은 **snake_case**: `place`, `user_favorite`, `congestion_forecast` (PostgreSQL에서 대문자는 큰따옴표를 강제하므로)
- 모든 FK에 `on delete cascade`
- **RLS 전 테이블 활성화** — `place`·`congestion_forecast`는 읽기 전체공개/쓰기는 service_role만, `user_favorite`은 `auth.uid() = user_id`로 본인 행만 조회·추가·삭제
- 인덱스: `user_favorite(user_id)`, `congestion_forecast(place_id, forecast_date)`
- 스키마 세부 근거는 PRD ⑥의 "구현 단계에서 확정한 사항" 참고

### 관계

- `auth.users` 1:N `UserFavorite`, `Place` 1:N `UserFavorite` → 결과적으로 users ↔ Place 는 **N:M**.
- `Place` 1:N `CongestionForecast` (한 장소가 시간대별로 여러 예측 행을 가짐).
- 관심 장소함 조회 순서: UserFavorite에서 해당 user_id의 place_id 목록 조회 → 그 id로 Place 조회.

### 테이블화하지 않는 데이터 (추가하지 말 것)

- **날씨**: 실시간성이 강해 테이블 대신 API 호출로 처리.
- **이동시간**("지하철 12분"): 지금은 목업 값. 카카오맵 길찾기 `mapUrl` 연동이 정확한 경로·시간을 대신하므로 급하지 않음.
- **GPS 좌표**: 저장하지 않음 (위 2번 참조).

---

## 4. MVP 스코프 원칙

1. **서비스 지역은 서울 한정.** 지역 선택 UI(시/군구 드롭다운·검색)는 **만들지 않는다.** 전국 확장은 Could로 백로그에만 존재한다.
2. **Must(PRD ④) 외의 기능은 먼저 묻지 않고 임의로 구현하지 않는다.** 필요해 보이면 제안만 하고 승인을 받는다.
   - **Must 5개**: ① 검색/조회 시 혼잡도 예측 표시 ② KTO 공공데이터 기반 혼잡 예측 로직(FastAPI) ③ 혼잡 시 대안 장소 리스트(복수 추천) ④ 대안 비교 뷰(접근성·날씨 등 나란히) ⑤ 관심 장소 다건 등록/스와이프 비교
   - **이미 반영하기로 결정된 Should**: 이동수단(도보/차) 토글 기반 판단 지원, 검색결과 체크박스 다중선택 담기.
   - **Could(보류)**: 동행자 공유 링크, 방문 시간대 사전 알림/찜, 일정 과밀도 체크, 지역 확장.
   - **Won't(제외)**: 통신사급 실시간 유동인구, 예약/결제 연동, AI 자동 일정 생성, 유모차/아동 동반 특화, 맛집 등 카테고리 확장, 별도의 "예상 혼잡 해소 시각" 텍스트(시간대별 그래프가 이미 대신함).
3. **성능 최적화를 먼저 하지 않는다.** 캐싱·CDN·부하분산·번들 최적화는 트래픽이 실제로 늘기 전까지 미룬다. 설계 기준 규모는 **개인 테스트 ~100명**이며, 이 규모에서 최적화는 과설계다.
   - 단, **구조 확장과 성능 최적화는 구분한다.** FastAPI 분리와 3테이블 구조는 이미 확정된 구조 결정이고, 순수 성능 최적화만 미루는 것이다.
4. 성공 지표는 퍼널 전환(단계별 이탈률)과 다중선택 담기 사용 비율. **재방문율/리텐션은 이번 단계에서 측정하지 않는다.**

---

## 5. 코딩 컨벤션

### 파일·폴더

- **컴포넌트 파일은 PascalCase**: `SearchResultCard.tsx`, `CongestionBadge.tsx`
- **유틸·훅 파일은 camelCase**: `useCongestion.ts`, `formatDistance.ts`
- **폴더는 타입별로 분리** (기능별 묶음 아님):
  ```
  components/   # UI 컴포넌트
  hooks/        # 커스텀 훅
  utils/        # 순수 함수 유틸
  types/        # 타입 정의
  ```

### 코드

- **TypeScript strict 모드**. `any` 남용 금지, 타입 단언보다 타입 가드 우선.
- **함수형 컴포넌트 위주.**
- **상태관리는 React 기본(useState / useContext)만 사용.** Zustand·Redux·Jotai 등 상태관리 라이브러리는 **도입 금지** — 필요하다고 판단되면 먼저 물어볼 것.
- **새 라이브러리 추가 전에는 반드시 먼저 물어본다.** (npm/pip 양쪽 모두)
- 변수명·함수명은 **영어**. 커밋 메시지·주석은 **한/영 혼용 가능**.

---

## 6. 작업 방식 (중요)

사용자는 **1인 개발자이자 실무 경력 1년 차**다. 다음을 지킨다.

### 설명 우선

- **설명 없이 큰 구조 변경을 하지 않는다.** 폴더 구조 변경, 데이터 흐름 변경, 추상화 추가 등은 **왜 그렇게 하는지 먼저 설명**하고 동의를 받은 뒤 진행한다.
- 코드를 낼 때는 "무엇을 했는지"뿐 아니라 **"왜 이 방식인지"**를 짧게 덧붙인다.

### 새 기능/화면 구현 전 — 인터뷰 먼저

바로 코드를 쓰지 말고, 아래를 **하나씩 물어보고 답을 받은 뒤** 진행한다.

1. 이 기능이 PRD의 어느 섹션(**Must / Should / Could**)에 해당하는지
2. **기존 컴포넌트·훅을 재사용**할지, **새로 만들**지
3. 애매한 부분은 **임의로 가정하지 말고 선택지를 제시해 고르게 한다** (A/B 형태로, 각 선택지의 트레이드오프를 한 줄씩)

### 반드시 사전 확인이 필요한 결정 (되돌리기 어려운 것)

- **스키마 변경** — 테이블/컬럼 추가·삭제·타입 변경, 제약조건 변경, 마이그레이션
- **새 라이브러리 도입** (npm, pip 공통)
- 배포 설정 변경, 환경변수 구조 변경
- 기존 파일 대량 삭제·이동

### 확인 없이 진행해도 되는 것 (여기까지만)

- 오타 수정
- **동작을 바꾸지 않는** 기존 로직 리팩터링
- 이미 합의된 내용의 단순 구현

이 세 가지 밖의 일은 기본적으로 **먼저 묻는다.**

---

## 참고

- 제품 근거 원본: `PRD.md`
- **진행상황·확정된 결정·다음 할 일: `PROGRESS.md`** — 세션을 새로 시작하면 여기부터 읽는다. 작업이 진행되면 이 파일을 갱신한다.
- 프로토타입: `prototype/` — 화면 8종(`01-landing.html` ~ `08-favorites-empty.html`)과 공용 `style.css`, `app.js`. `prototype/index.html`은 8개 화면을 한눈에 보는 목록 페이지다.
  - 화면 구성과 흐름의 **레퍼런스**이지 이식 대상 코드가 아니다. Next.js로 옮길 때 그대로 복붙하지 말고, 어떤 부분을 컴포넌트로 나눌지 먼저 상의한다.
  - `prototype/` 안의 파일은 앱 빌드에 포함되지 않는다. Next.js 셋업 시 이 폴더를 빌드·린트 대상에서 제외한다.
